const RATE_BASE="https://open.er-api.com/v6/latest";
const HISTORY_BASE="https://api.frankfurter.dev/v1";
const RANGE_DAYS={"1W":7,"1M":30,"3M":90,"1Y":365};
export async function getRates(fromCurrency){const response=await fetch(`${RATE_BASE}/${fromCurrency}`);if(!response.ok)throw new Error("Failed to fetch exchange rate");const data=await response.json();return {rates:data.rates,lastUpdated:data.time_last_update_utc};}
export async function getHistoricalRange(fromCurrency,toCurrency,rangeKey){const days=RANGE_DAYS[rangeKey]??7;const end=new Date();const start=new Date();start.setDate(end.getDate()-days);const startStr=start.toISOString().split("T")[0];const endStr=end.toISOString().split("T")[0];try{const response=await fetch(`${HISTORY_BASE}/${startStr}..${endStr}?base=${fromCurrency}&symbols=${toCurrency}`);if(!response.ok)return null;const data=await response.json();const entries=Object.entries(data.rates||{}).sort(([a],[b])=>a.localeCompare(b));if(entries.length<2)return null;const points=entries.map(([date,r])=>({date,value:r[toCurrency]})).filter(p=>typeof p.value==="number");if(points.length<2)return null;const first=points[0].value,last=points.at(-1).value;const high=Math.max(...points.map(p=>p.value));const low=Math.min(...points.map(p=>p.value));return {points,first,last,high,low,changePercent:((last-first)/first)*100};}catch{return null}}
